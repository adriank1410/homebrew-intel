#!/bin/bash
# SPDX-License-Identifier: BSD-2-Clause
set -euo pipefail
script_dir="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd -P)"
cd -- "$script_dir/.."
exec /usr/bin/python3 -m intelbrew.cli "$@"
